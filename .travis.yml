sudo: required

services:
  - docker

env:
  - PYTHON_VERSION=3.6
  - PYTHON_VERSION=3.7

script:
  - make "$PYTHON_VERSION"
  - >
    docker run
    -it
    -p 8000:8000
    -v "$(pwd):/var/www/app"
    --entrypoint /var/www/app/test.sh
    "python-container:$PYTHON_VERSION"
